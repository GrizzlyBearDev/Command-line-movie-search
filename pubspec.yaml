name: movie
description: A simple command-line movie search application using OMDB.
# version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.8.1 <3.0.0'

dependencies:
  http: ^0.12.2
  dbus: ^0.1.0
  dotenv: ^2.0.0
  ansicolor: ^1.1.1

dev_dependencies:
  pedantic: ^1.9.0
